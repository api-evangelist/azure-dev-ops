naftiko: 1.0.0-alpha2
info:
  label: Azure DevOps — Operations
  description: 'Azure DevOps — Operations. 1 operations. Lead operation: Azure DevOps Operations_ List. Self-contained Naftiko
    capability covering one Azure Dev Ops business surface.'
  tags:
  - Azure Dev Ops
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AZURE_DEV_OPS_API_KEY: AZURE_DEV_OPS_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-dev-ops-operations
    baseUri: https://management.azure.com
    description: Azure DevOps — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.DevOps-operations
      path: /providers/Microsoft.DevOps/operations
      operations:
      - name: operationslist
        method: GET
        description: Azure DevOps Operations_ List
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: azure-dev-ops-operations-rest
    port: 8080
    description: REST adapter for Azure DevOps — Operations. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/providers/microsoft-devops/operations
      name: providers-microsoft-devops-operations
      description: REST surface for providers-Microsoft.DevOps-operations.
      operations:
      - method: GET
        name: operationslist
        description: Azure DevOps Operations_ List
        call: azure-dev-ops-operations.operationslist
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-dev-ops-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure DevOps — Operations. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: azure-devops-operations-list
      description: Azure DevOps Operations_ List
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: azure-dev-ops-operations.operationslist
      outputParameters:
      - type: object
        mapping: $.
